// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   /* config options here */
//   domains: ["images.unsplash.com", "img.clerk.com"],
// };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "img.clerk.com"],
  },
};

export default nextConfig;